# Given an Array of Integers Arr and number K. Return the maximum of a Sub-array of size K.

Arr = int(input("Enter the Array length: "))

arr = int(input("Enter the values of the Array: "))

for i in Arr:
    
