i = 1

while i<=5:
    print(f"We Doing Nested Loops(incrementing): {i}")
    i =i+1

    j=5
    while j>= 1:
        print(f"Loop under Loop(Decrementing): {j}") 
        j= j- 1

# Nested Loop (Loop under Loop) 
# we can se that j is starting from 1 in every loop because it is declared inside so j=1 for every i.
# We can get single j for 1 value by declaring out of While and by that we can do it for only first i only.  
